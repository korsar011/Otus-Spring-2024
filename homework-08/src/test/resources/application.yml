spring:
  profiles:
    active: test
  data:
    mongodb:
      port: 0
de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.0.2

mongock:
  runner-type: "InitializingBean"
  change-logs-scan-package:
    - ru.otus.hw.testChangelogs
  mongo-db:
    write-concern:
      journal: false
    read-concern: local
