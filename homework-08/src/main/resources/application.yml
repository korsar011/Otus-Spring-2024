spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: admin
      username: root
      password: root
  shell:
    interactive:
      enabled: true
    noninteractive:
      enabled: false

    command:
      version:
        enabled: false

mongock:
  runner-type: "InitializingBean"
  change-logs-scan-package:
    - ru.otus.hw.changelogs
